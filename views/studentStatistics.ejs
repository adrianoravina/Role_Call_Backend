<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Teacher Blocks</title>

    <style type="text/css">
        .div-table
        {
            display:  table;
            width:auto;
            background-color:#eee;
            border:1px solid  #666666;
            border-spacing:5px;/*cellspacing:poor IE support for  this*/
           /* border-collapse:separate;*/
        }
    
        .row
        {
           display:table-row;
           width:auto;
        }
    
        .col
        {
            float:left;/*fix for  buggy browsers*/
            display:table-column;
            width:200px;
            background-color:#ccc;
        }
    </style>
</head>
<body>
    <header class="bigHeader" style="position: relative;">
        <h1>Student Statistics</h1>
        <div class="headerStyle"> <button class="logOut">Log Out</button></div>
        <div class="navBar">
            <a href="/studentPage">Student Page</a>
            <a href="#">Statistics Page</a>
        </div>  
    </header>

    <form action="" method="post">
        <div class="div-table">
            <div class="header-row">
                <div class="col">Block Id</div>
                <div class="col">Duration</div>
                <div class="col">Status</div>
                <div class="col"></div>
            </div>
            <% for(var i=0; i < data.length; i++) { %>
            <div class="row">
                <div class="col"><%= data[i].block_id %></div>
                <div class="col"><%= data[i].b_duration_min %></div>
                <div class="col"> In Session </div>
                <div class="col"><button type="submit" name="blockId" value=<%= data[i].block_id %>>Start Block</button></div>
            </div>
            <% } %>
        <div class="footer"></div>
    </div>
</form>
    
</body>
</html>